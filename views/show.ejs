<% include ./partials/header.ejs %>
    <div class="w-full min-h-screen bg-zinc-700 px-10 py-8 text-white">
        <div class="cards flex flex-wrap gap-10 ">
            <% user.posts.forEach(function(elem){ %>
                <div class="card w-38">
                    <h2 class="mt-2 text-sm mx-20"><%= elem.title %></h2>

                    <div class=" w-full h-40 bg-sky-200 rounded-lg overflow-hidden">
                        <img src="/images/uploads/<%= elem.image %>" class="w-full h-full object-cover" alt="">
                    </div>
                    <h5 class="mt-2 text-sm "><%= elem.imageText %></h5>
                </div>    
           <% }) %>
        </div>
    </div>
<% include ./partials/footer.ejs %>
